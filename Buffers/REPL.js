REPL stands for Read Eval Print Loop, and it represents a computer environment

Read - reads a user input, parses input into javascript datastructure, stores into a memory
eval - takes and evaluates the data strucutre.
print - prints the user input on console
loop - repeats the above procedure until the user presses ctrl+c twice.
